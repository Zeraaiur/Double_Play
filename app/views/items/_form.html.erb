<%= simple_form_for [@item] do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :price %>
  <%= f.input :photo %>
  <%= f.input :user_id, :as => :hidden, :value => current_user %>
  <%= f.button :submit %>
<% end -%>
